# Week 6 Starter

This is the Week 5 Complete Short Film Programme Builder. Selection lives in
`App`, the count and running time are derived, and the summary tells the truth.

The application now uses all 180 films from the connected fictional archive.
The records include richer taxonomy, filmmaker, company and honours data, plus
deliberate gaps. The interface uses only the fields it needs, but the fuller
records make this state a useful starting point for learner exploration.

Its deliberate problem is simple: choose a programme and refresh the browser.
The selection disappears because React state belongs to the running
application.

```bash
npm ci
npm run dev
```
